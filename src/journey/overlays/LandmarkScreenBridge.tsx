/**
 * Callout screen anchors are published by NavCompanionBot (line from robot → panel).
 * Kept as a no-op mount so older scene wiring stays valid.
 */
export function LandmarkScreenBridge() {
  return null;
}
